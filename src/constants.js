const rangeTypes = { TEMP: 'temp' };

const modeTypes = { DEFAULT: 'default' };

const deviceTypes = { OVEN: 'oven', ROBOT_HOVER: 'robot-hoover' };

module.exports = {
  rangeTypes,
  modeTypes,
  deviceTypes,
};
