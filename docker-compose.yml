version: '3'
services:
  security:
    build: .
    ports:
      - '3001:3001'
    depends_on:
      - db
    environment:
      - DATABASE_HOST=db
    volumes:
      - ./sample-service/api:/app/api
  db:
    build: ./database-service
